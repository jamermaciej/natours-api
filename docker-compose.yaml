version: '3.9'
services:
  express:
    build:
      context: ./
    ports:
      - '3000:3000'
    volumes:
      - $PWD:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command:
      - nodemon
      - server.js